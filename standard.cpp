#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int N = 0;
	cin >> N;

    int d = 0;
	vector<int> D(N);
	for (int i { 0 }; i < N; ++i) {
		cin >> d;
		D.push_back(d);
	}
    
  cout << s << endl;  // *
  cout << a << endl;  // 1
}
