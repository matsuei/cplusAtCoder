#include <bits/stdc++.h>
#include <iostream>
using namespace std;
 
int main() {
  string s;
  cin >> s;
  
  string pre = s.substr(0, n);
  string post = s.substr(n);
  
  // 参照先の入れ替え
  swap(pre[a], pre[b])
  swap(pre, post)
  cout << pre << post << endl;
}
